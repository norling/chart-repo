apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chart-repo.fullname" . }}-test-connection"
  labels:
    {{- include "chart-repo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chart-repo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
